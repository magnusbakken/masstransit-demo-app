We want to add support for OpenTelemetry. MassTransit should have good built-in support for exporting traces and metrics through OpenTelemetry.

We'll set up a local instance of Grafana, which should be added to the Docker Compose file, and set up MassTransit to export as much useful data as possible.
